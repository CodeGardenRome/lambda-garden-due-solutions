(ns tic-tac-toe.lib)
;; matrice -> get-indici-righe >> get-indici-colonne >> get-indici-diagonali ->
;; riduci risultato per ogni funzione (false|vincitore) -> riduci le
;; riduzioni (stallo|vincitore)

(defn check-line
  [line]
  "Implementami!")
